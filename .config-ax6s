# ==============================================================================
# Target & Kernel (MTK7622 极限编译器压榨)
# ==============================================================================
CONFIG_TARGET_mediatek=y
CONFIG_TARGET_mediatek_mt7622=y
CONFIG_TARGET_mediatek_mt7622_DEVICE_xiaomi_redmi-router-ax6s=y
CONFIG_LINUX_6_12=y
CONFIG_EXPERIMENTAL=y
CONFIG_TARGET_ROOTFS_INITRAMFS=y
CONFIG_TARGET_INITRAMFS_COMPRESSION_LZMA=y
CONFIG_USES_FIT_IMAGE=y

# 【激进现代化思维】放弃体积优化，全面拥抱 O3 最高速编译与 LTO 链路优化
CONFIG_TARGET_OPTIMIZATION="-O3 -pipe -march=armv8-a+crypto+crc"
CONFIG_USE_LTO=y

# 彻底激活 MT7622 硬件潜能
CONFIG_PACKAGE_kmod-mt7622-hwnat=y
CONFIG_PACKAGE_kmod-nft-offload=y
CONFIG_PACKAGE_kmod-crypto-hw-safexcel=y
CONFIG_PACKAGE_kmod-tcp-bbr=y

# ==============================================================================
# Base System & Modern Security (无线协议栈精细化瘦身)
# ==============================================================================
CONFIG_PACKAGE_bash=y
CONFIG_PACKAGE_ca-bundle=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_LUCI_LANG_zh_Hans=y
CONFIG_PACKAGE_luci-ssl-openssl=y

# 替换大而无当的 wpad-openssl，保留 WPA3/漫游/Mesh，斩断企业级冗余
CONFIG_PACKAGE_wpad-basic-openssl=y
# CONFIG_PACKAGE_wpad-openssl is not set

# ==============================================================================
# Network Core (100% Pure nftables + Dnsmasq 子模块微雕)
# ==============================================================================
CONFIG_PACKAGE_dnsmasq-full=y
# CONFIG_PACKAGE_dnsmasq is not set
CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y

# Dnsmasq-full 编译期裁剪：彻底移除 ipset/tftp，全面拥抱 nftset
CONFIG_PACKAGE_dnsmasq_full_nftset=y
# CONFIG_PACKAGE_dnsmasq_full_ipset is not set
# CONFIG_PACKAGE_dnsmasq_full_tftp is not set
# CONFIG_PACKAGE_dnsmasq_full_auth is not set

CONFIG_PACKAGE_ip-full=y
CONFIG_IPV6=y
CONFIG_KERNEL_IPV6=y
CONFIG_PACKAGE_kmod-nft-netdev=y
CONFIG_PACKAGE_kmod-nft-tproxy=y
CONFIG_PACKAGE_kmod-tun=y

# 纠偏：必须使用 mtk 专属硬件全锥形 NAT 模块，确保不破坏 PPE 硬件加速
CONFIG_PACKAGE_kmod-nft-fullcone-mtk=y
# CONFIG_PACKAGE_kmod-nft-fullcone is not set

# ==============================================================================
# Modern Proxy & DNS (PassWall + Sing-Box 核心一体化)
# ==============================================================================
CONFIG_PACKAGE_luci-app-passwall=y
CONFIG_PACKAGE_luci-app-passwall_INCLUDE_SingBox=y

# ==============================================================================
# LuCI Applications & Firewall4 纯净流
# ==============================================================================
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-theme-argon-mod=y
CONFIG_PACKAGE_luci-app-autoupdate=y
CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_luci-app-wol=y

# 【架构隐患提示】serverchan/onliner 含有高频轮询 Shell 脚本，若非刚需建议不装
CONFIG_PACKAGE_luci-app-onliner=y
CONFIG_PACKAGE_luci-app-serverchan=y

# UPnP & VPN & DDNS 现代化 nftables 修正组合
CONFIG_PACKAGE_luci-app-upnp=y
CONFIG_PACKAGE_miniupnpd-nftables=y
CONFIG_PACKAGE_luci-app-wireguard=y
CONFIG_PACKAGE_luci-app-natter=y
CONFIG_PACKAGE_luci-app-ddns=y
CONFIG_PACKAGE_ddns-scripts_cloudflare.com-v4=y

# ==============================================================================
# Utilities & Command Line
# ==============================================================================
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_jq=y

# ==============================================================================
# Legacy / Bloatware Clean
# ==============================================================================
# CONFIG_PACKAGE_luci-compat is not set
